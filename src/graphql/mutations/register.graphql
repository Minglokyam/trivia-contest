mutation Register($options: CredentialOptions!){
    register(options: $options){
        user {
            id
            username
        }
        errors {
            field
            message
        }
    }
}